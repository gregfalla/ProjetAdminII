options {
        directory "/var/cache/bind";
        listen-on { any; };
        listen-on-v6 { none; };
        allow-recursion {
                none;
        };
        allow-transfer {
                none;
        };
        allow-update {
                none;
        };
};

zone "l2-1.ephec-ti.be." {
    type primary;
    file "/var/lib/bind/l2-1.ephec-ti.be";
};
